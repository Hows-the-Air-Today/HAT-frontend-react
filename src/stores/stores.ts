import { atom } from "recoil";

export const locationState = atom({
  key: "locationState",
  default: null,
});

export const tmCoordState = atom({
  key: "tmCoordState",
  default: null,
});

export const addressState = atom({
  key: "addressState",
  default: null,
});
